<h1>Reservations#show</h1>

<% if flash[:success] %>
  <p class="success"><%= flash[:success] %></p>
<% end %>
user is <%= @reservation.user %>
<table>
  <tr>
    <th>Restaurant</th><td><%= @restaurant.name %></td>
  </tr><tr>
    <th>Time</th><td><%= @reservation.time %></td>
  </tr><tr>
    <th>Party size</th><td><%= @reservation.party_size %></td>
  </tr>

</table>

<%= link_to "Cancel Reservation", @reservation, method: :delete %>
